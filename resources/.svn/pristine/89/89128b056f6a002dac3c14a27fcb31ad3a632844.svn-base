/*---------------------------------------------------------- 
  default input styling
----------------------------------------------------------*/
.input-icon{
	position: relative;

	.input{
		padding-right: ($gutter * 2);
	}

	&:before{
		position: absolute;
		right: 10px;
		top: 2px;
		display: block;
	}

	&.disabled{
    cursor: not-allowed;
	}
}

/*---------------------------------------------------------- 
  if required mulitple input variation.
----------------------------------------------------------*/
@if $inputIconColorOption{
	@include inputIconVariation(primary, $color-primary);
	@include inputIconVariation(secondary, $color-secondary);
	@include inputIconVariation(error, $color-error);
	@include inputIconVariation(success, $color-success);
}